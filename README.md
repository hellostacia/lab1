# Лабораторная работа №1

Запуск:

```bash
python -m venv ve
ve\Scripts\activate
pip install -r requirements.txt
cd app
flask --app app run
```

Готовая страница поста находится по адресу `/posts/0`.
